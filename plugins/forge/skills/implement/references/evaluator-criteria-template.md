# Evaluator Criteria — [Project Name]

> Auto-generated by forge-investigator.
> Edit this file to add project-specific rules.
> Referenced by both forge-generator and forge-evaluator.


## Sources

Reference existing documentation instead of duplicating rules.
The evaluator reads these sources for full context.

- `[path/to/CLAUDE.md]` — [what conventions it defines]
- `[path/to/rules/]` — [what rules exist]
- `[path/to/skills/]` — [what patterns skills enforce]


## Available Tools

The evaluator detects these at runtime.
If a tool becomes unavailable, the evaluator skips its checks and notes it in the report.

| Tool | Detection | Check command | Fallback |
|------|-----------|--------------|----------|
| Build | [nx/mvn/gradle/npm] | [command] | Skip, note in report |
| Tests | [jest/junit/pytest] | [command] | Skip, note in report |
| Lint | [eslint/spotless/ruff] | [command] | Skip, note in report |
| SonarQube | env SONARQUBE_URL | analyze_file_list MCP | Skip, note in report |


## Tier 1 — BLOCKER (fails evaluation)

Skip anything already enforced by linters, formatters, or compilers.
Only check what those tools miss.

### Static Checks (grep)

| # | Rule | File pattern | Pattern to grep | Source |
|---|------|-------------|----------------|--------|
| 1 | [convention] | [*.ext] | [pattern] | [CLAUDE.md:line] |

### Protected Files

| # | Rule | Check |
|---|------|-------|
| | [No manual edits to generated files] | git diff on [path] |

### Dynamic Checks (tool-dependent)

| # | Rule | Command | Tool required |
|---|------|---------|--------------|
| | Build compiles | [build command] | [build tool] |
| | Tests pass | [test command] | [test runner] |
| | Lint passes | [lint command] | [linter] |


## Tier 2 — WARNING (reported but doesn't fail)

| # | Rule | Check | Source |
|---|------|-------|--------|
| | [quality convention] | [how to check] | [source doc] |


## Evaluation Output Format

The evaluator MUST return findings in the standard forge format:
Verdict (PASS/FAIL), BLOCKERs, WARNINGs, Dynamic Checks, Tool Availability, Summary.


## Convergence Rules

- MAX_ITERATIONS = 3
- Only BLOCKERs cause FAIL
- Each iteration must reduce BLOCKER count — if not, escalate
- Dynamic checks run only on final iteration (too expensive for every round)
